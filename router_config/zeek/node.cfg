# Zeek node configuration – BSD router standalone deployment
#
# Monitors the LAN interface (em1) where TLS ClientHellos originate from
# individual LAN clients before IPv4 NAT obscures source addresses.
# Change 'interface' to 'em0' to fingerprint WAN-side connections instead.

[zeek]
type=standalone
host=localhost
interface=em1
